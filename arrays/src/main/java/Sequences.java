/**
 * Functions related to sequences.
 */
public class Sequences {
    /*
     * Generate min number without repeating digits from sequence expressed in D(decreasing) and I(increasing)
     */
    long generateSeqeunce(String pattern) {
        /*
         * 1, 0, 2, -1, 3
         * D, I, D, I
         *
         */
        return 0;
    }
}
